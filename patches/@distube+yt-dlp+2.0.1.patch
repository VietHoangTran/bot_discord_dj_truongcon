diff --git a/node_modules/@distube/yt-dlp/dist/index.js b/node_modules/@distube/yt-dlp/dist/index.js
index b17b126..62ae470 100644
--- a/node_modules/@distube/yt-dlp/dist/index.js
+++ b/node_modules/@distube/yt-dlp/dist/index.js
@@ -73,16 +73,25 @@ var args = /* @__PURE__ */ __name((url, flags = {}) => [url].concat((0, import_d
 var json = /* @__PURE__ */ __name((url, flags, options) => {
   const process2 = (0, import_node_child_process.spawn)(YTDLP_PATH, args(url, flags), options);
   return new Promise((resolve, reject) => {
-    let output = "";
+    // Tách stdout/stderr: yt-dlp có thể exit non-zero (vd 1 entry trong playlist
+    // bị bot-detect trên IP datacenter) nhưng vẫn in JSON hợp lệ ra stdout. Ưu
+    // tiên parse stdout bất kể exit code -> không drop cả playlist chỉ vì 1 bài.
+    let stdout = "";
+    let stderr = "";
     process2.stdout?.on("data", (chunk) => {
-      output += chunk;
+      stdout += chunk;
     });
     process2.stderr?.on("data", (chunk) => {
-      output += chunk;
+      stderr += chunk;
     });
     process2.on("close", (code) => {
-      if (code === 0) resolve(JSON.parse(output));
-      else reject(new Error(output));
+      if (stdout) {
+        try {
+          return resolve(JSON.parse(stdout));
+        } catch {
+        }
+      }
+      reject(new Error(stderr || stdout || `yt-dlp exited with code ${code}`));
     });
     process2.on("error", reject);
   });
@@ -144,19 +153,24 @@ var YtDlpPlugin = class extends import_distube.PlayableExtractorPlugin {
     const info = await json(url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
-      simulate: true
+      simulate: true,
+      ignoreErrors: true,
+      extractorArgs: "youtube:player_client=tv,ios,mweb,android",
+      cookies: process.env.YOUTUBE_COOKIES_FILE || "/app/cookies.txt"
     }).catch((e2) => {
       throw new import_distube.DisTubeError("YTDLP_ERROR", `${e2.stderr || e2}`);
     });
     if (isPlaylist(info)) {
-      if (info.entries.length === 0) throw new import_distube.DisTubeError("YTDLP_ERROR", "The playlist is empty");
+      // Bỏ các entry null (entry bị lỗi khi --ignore-errors, vd bot-detect) trước
+      // khi map thành YtDlpSong, tránh crash `new YtDlpSong(this, null, ...)`.
+      const songs = info.entries.filter((i) => i).map((i) => new YtDlpSong(this, i, options));
+      if (songs.length === 0) throw new import_distube.DisTubeError("YTDLP_ERROR", "The playlist is empty");
       return new import_distube.Playlist(
         {
           source: info.extractor,
-          songs: info.entries.map((i) => new YtDlpSong(this, i, options)),
+          songs,
           id: info.id.toString(),
           name: info.title,
           url: info.webpage_url,
@@ -174,11 +188,12 @@ var YtDlpPlugin = class extends import_distube.PlayableExtractorPlugin {
     const info = await json(song.url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
       simulate: true,
-      format: "ba/ba*"
+      format: "ba/ba*",
+      extractorArgs: "youtube:player_client=tv,ios,mweb,android",
+      cookies: process.env.YOUTUBE_COOKIES_FILE || "/app/cookies.txt"
     }).catch((e2) => {
       throw new import_distube.DisTubeError("YTDLP_ERROR", `${e2.stderr || e2}`);
     });
diff --git a/node_modules/@distube/yt-dlp/dist/index.mjs b/node_modules/@distube/yt-dlp/dist/index.mjs
index bf07337..4579730 100644
--- a/node_modules/@distube/yt-dlp/dist/index.mjs
+++ b/node_modules/@distube/yt-dlp/dist/index.mjs
@@ -44,16 +44,25 @@ var args = /* @__PURE__ */ __name((url, flags = {}) => [url].concat(dargs(flags,
 var json = /* @__PURE__ */ __name((url, flags, options) => {
   const process2 = spawn(YTDLP_PATH, args(url, flags), options);
   return new Promise((resolve, reject) => {
-    let output = "";
+    // Tách stdout/stderr: yt-dlp có thể exit non-zero (vd 1 entry trong playlist
+    // bị bot-detect trên IP datacenter) nhưng vẫn in JSON hợp lệ ra stdout. Ưu
+    // tiên parse stdout bất kể exit code -> không drop cả playlist chỉ vì 1 bài.
+    let stdout = "";
+    let stderr = "";
     process2.stdout?.on("data", (chunk) => {
-      output += chunk;
+      stdout += chunk;
     });
     process2.stderr?.on("data", (chunk) => {
-      output += chunk;
+      stderr += chunk;
     });
     process2.on("close", (code) => {
-      if (code === 0) resolve(JSON.parse(output));
-      else reject(new Error(output));
+      if (stdout) {
+        try {
+          return resolve(JSON.parse(stdout));
+        } catch {
+        }
+      }
+      reject(new Error(stderr || stdout || `yt-dlp exited with code ${code}`));
     });
     process2.on("error", reject);
   });
@@ -115,19 +124,24 @@ var YtDlpPlugin = class extends PlayableExtractorPlugin {
     const info = await json(url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
-      simulate: true
+      simulate: true,
+      ignoreErrors: true,
+      extractorArgs: "youtube:player_client=tv,ios,mweb,android",
+      cookies: process.env.YOUTUBE_COOKIES_FILE || "/app/cookies.txt"
     }).catch((e2) => {
       throw new DisTubeError("YTDLP_ERROR", `${e2.stderr || e2}`);
     });
     if (isPlaylist(info)) {
-      if (info.entries.length === 0) throw new DisTubeError("YTDLP_ERROR", "The playlist is empty");
+      // Bỏ các entry null (entry bị lỗi khi --ignore-errors, vd bot-detect) trước
+      // khi map thành YtDlpSong, tránh crash `new YtDlpSong(this, null, ...)`.
+      const songs = info.entries.filter((i) => i).map((i) => new YtDlpSong(this, i, options));
+      if (songs.length === 0) throw new DisTubeError("YTDLP_ERROR", "The playlist is empty");
       return new Playlist(
         {
           source: info.extractor,
-          songs: info.entries.map((i) => new YtDlpSong(this, i, options)),
+          songs,
           id: info.id.toString(),
           name: info.title,
           url: info.webpage_url,
@@ -145,11 +159,12 @@ var YtDlpPlugin = class extends PlayableExtractorPlugin {
     const info = await json(song.url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
       simulate: true,
-      format: "ba/ba*"
+      format: "ba/ba*",
+      extractorArgs: "youtube:player_client=tv,ios,mweb,android",
+      cookies: process.env.YOUTUBE_COOKIES_FILE || "/app/cookies.txt"
     }).catch((e2) => {
       throw new DisTubeError("YTDLP_ERROR", `${e2.stderr || e2}`);
     });
